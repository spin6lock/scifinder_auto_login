scifinder_auto_login
====================

an autohotkey scripts to log in scifinder automatically. It just replace the hand click process so I expect that it won't be ban by sci, but use it *at your own risk*